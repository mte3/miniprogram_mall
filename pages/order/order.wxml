<!--pages/order/order.wxml-->
<view class="orderSure">
	<m-orderAdress adressDetail="{{adressDetail}}" adress="{{adress}}" />
	<m-orderDetail order="{{order}}" cartList="{{cartList}}" orderGood="{{orderGood}}" isMore="{{isMore}}" />
	<m-orderSubmit class="submit" pay = "{{pay}}"/>
</view>